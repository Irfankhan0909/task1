pipeline {
    agent any

    stages {
        stage('Cloning') {
            steps {
                git 'https://github.com/Irfankhan0909/task1.git'
            }
        }

        stage('Deploying') {
            steps {
                    sh 'rsync -a /var/lib/jenkins/workspace/task1 root@172.31.7.83:/root/'
            }
        }
    }
}
